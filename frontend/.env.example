# URL of the Obscura indexer used for vault discovery and Merkle proof generation.
VITE_INDEXER_URL=http://localhost:3001
# Read-only Ethereum JSON-RPC endpoint used for log queries / fallback tree
# reconstruction (Sepolia). Defaults to https://1rpc.io/sepolia when unset.
# VITE_RPC_URL=https://1rpc.io/sepolia
